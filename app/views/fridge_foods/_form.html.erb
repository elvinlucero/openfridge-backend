<%= form_for(@fridge_food) do |f| %>
  <% if @fridge_food.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@fridge_food.errors.count, "error") %> prohibited this fridge_food from being saved:</h2>

      <ul>
      <% @fridge_food.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :desc %><br />
    <%= f.text_field :desc %>
  </div>
  <div class="field">
    <%= f.label :expiration %><br />
    <%= f.date_select :expiration %>
  </div>
  <div class="field">
    <%= f.hidden_field :user_id, :value => session[:user_id] %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
