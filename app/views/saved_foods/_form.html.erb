<%= form_for(@saved_food) do |f| %>
  <% if @saved_food.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@saved_food.errors.count, "error") %> prohibited this saved_food from being saved:</h2>

      <ul>
      <% @saved_food.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :desc %><br />
    <%= f.text_field :desc %>
  </div>
  <div class="field">
    <%= f.label :offset %><br />
    <%= f.text_field :offset %>
  </div>
  <div class="field">
    <%= f.hidden_field :user_id, :value => session[:user_id] %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
